name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  swift-test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Run Swift tests
        run: swift test
